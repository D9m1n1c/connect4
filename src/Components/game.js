/*
when clicked on to the column, set the bottom of the column to new state
*/

import { useState } from "react";

function DropToken(playerID) {
  const [bottom, setBottom] = useState("");
}

// update the content in the button from the bottom
